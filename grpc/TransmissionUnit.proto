syntax = "proto3";
package unit;
import "ComputationMessage.proto";
import "TransmissionMessage.proto";
service DNet {
    rpc Compute (unit.ComputationRequest) returns (unit.ComputationResponse);
    rpc Qlength (unit.QlengthRequest) returns (unit.QlengthResponse);
}