apiVersion: apps/v1
kind: Deployment
metadata:
  name: dnet-transmissionunit-12
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dnet-transmissionunit-12
  template:
    metadata:
      name: dnet-transmissionunit-12-pod
      labels:
        app: dnet-transmissionunit-12
    spec:
      containers:
        - name: dnet-transmissionunit
          image: yindaheng98/dnet-transmissionunit
          ports:
            - containerPort: 8080
          command: ["npm"]
          args:
            - "run"
            - "start"
            - "--"
            - "-a"
            - "amqp://dnet-queue"
            - "-q"
            - "q12"
            - "-n"
            - "dnet-unit-16:8080"
---
apiVersion: v1
kind: Service
metadata:
  name: dnet-unit-12
spec:
  selector:
    app: dnet-transmissionunit-12
  ports:
  - name: dnet-transmissionunit
    protocol: TCP
    port: 8080
    targetPort: 8080
